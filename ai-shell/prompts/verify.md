# Verify profile

Read `../../AGENTS.md` and
`../../.github/prompts/canonical/verification.prompt.md`. Run the narrowest
executable authority under `../../tools/agent/`, record exit codes and logs,
and finish with `git diff --check` and repository status.
