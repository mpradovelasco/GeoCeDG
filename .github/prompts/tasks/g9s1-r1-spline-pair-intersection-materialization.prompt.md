# G9S1-R1 — SplineV2 Pair Intersection Materialization

**AUTHOR-AUTHORIZED BOUNDED IMPLEMENTATION — NOT PHASE PASS.**

Current execution status: **IMPLEMENTATION CANDIDATE — PENDING AUTHOR REVIEW**.
`implementationComplete=true`. The earlier composed run exposed quintic
admission and false transverse implicit-root regressions. Authorized corrections
A/B are resolved on the tested cohort: PHASE A/B each pass 192/192 with identical
canonical summary, COMPOSED passes 1281/1281, and FULL `-CleanBuild` exits 0
with 7781 tests, 7770 passed, 11 retained upstream skips and zero failures.
Original failed cohorts remain preserved, not waived or relabeled. DEV alone
was not acceptance; the required fresh technical gates have now completed.
See the current structural implementation report and ADR 0022.
Technical completion is supported by the tested-cohort receipts recorded in the
implementation matrix. This status-only prompt update is not a new product
source cohort or an author approval. The author
has disposed of the previous scientific stop by
authorizing the bounded structural spline representation correction in ADR 0022,
then completion of the original R1 contract. This does not authorize weaker
certification, a new interpolation family or a diagnostic substitute. Preserve
the original failed execution and exact knot discrepancy. The
[blocker report](../../../docs/validation/g9s1_r1_implementation_blocker_report.md)
and [saved blocker evidence](../../../geocedg/validation/g9s1-r1/g9s1-r1-implementation-blocker-evidence.json)
remain immutable historical records; resumed validation belongs to new evidence.
Technical completion is recorded; phase PASS and author approval remain absent.

# Objective

Implement the author-approved D2 partial singleton-transverse-germ contract for
SplineV2 pair exact-token materialization. Retain the accepted Disposition C
monodromy finding and all earlier negative evidence. Deliver an implementation
candidate for author review, not an unrestricted pair-sheet selector.

# Authority and evidence hierarchy

Use current code/tests/serialization, accepted ADRs/specifications, analytic
controls and saved evidence in that order. Entry main/origin/direct remote is
`109f077fc5e2a40bcde45d3271eb928ee66fdfcc`; preserve its R4/R5/G9S1/R6 tags and
the current uncommitted D2 work on `codex/g9s1-r1-spline-pair-materialization`.
The protected G9U1 design checkpoint
`00982e7e148a634cd57ed928f322774df267d5e3` is read-only; do not import its branch.

Record the explicit author decision: D2 design approved, Disposition C accepted
for the proved scope, B partial implementation authorized, semantic-slot
reactivation accepted; `generalRegularPairInvalidation=REJECTED`. Design
approval is not `authorApprovedPhase` or a PASS claim. Preserve the 13-path D2
archive and its hashes; update only living authorities and new implementation
records. The D2 diagnostic trace
`101cba5816b57b48feb2bd9a2185841809115331fc11a6421ea78550b5797354`
is not a production certificate.

# Scope

Authenticated SplineV2 x SplineV2, including currently supported certifiable
similarity compositions; strict local proof/class coverage, symmetric structural
selector, existing ledger/point lifecycle integration, persistence, tests and
the bounded operational/documentary authority required by this phase.
This includes the author-authorized structural SplineV2 prerequisite: direct
equivalent-basis solving, exact periodic elimination and an interval bridge for
the represented structural function. Preserve the existing bounded public
family and all original R1 requirements; this is not a new phase.

# Explicitly forbidden scope

No generic non-Spline LocusV2 pair promotion; Cartesian/proximity/list/rank/history
identity; arbitrary atlas cut; uncertified materialization policy; R4 phase-tube
reuse as pair semantics; silent old diagnostic-token migration; frontend Point
tool, candidate markers or workspace implementation. Do not execute G9U1,
modify protected planning history, commit, push, merge or tag.

# Architectural placement

The correction belongs in the shared Java semantic kernel. Reuse current
polynomial capability, symmetric query, rich result, exact-token ledger and
ordinary GeoPoint parent through their normal DAG/persistence seams. Keep one
query solve/certification pass, then direct selector lookup per retained point.
Do not introduce a second point model or independent coordinate-token system.

# Required design/specification

- [ADR 0021](../../../docs/adr/0021-spline-pair-singleton-germ-materialization.md).
- [ADR 0022](../../../docs/adr/0022-structural-spline-continuity.md),
  [structural design](../../../docs/architecture/g9s1_r1_structural_spline_continuity.md)
  and [successor research note](../../../docs/research/g9s1_r1_structural_spline_numerics.md).
- [Normative partial contract](../../../geocedg/specs/curves/spline-v2-pair-materialization.md).
- [Preserved D2 design](../../../docs/architecture/g9s1_r1_d2_pair_sheet_contract.md).
- [Implementation matrix](../../../docs/validation/g9s1_r1_spline_pair_materialization_validation_matrix.md).
- [ADR 0009](../../../docs/adr/0009-locus-v2-locus-intersection-pair-semantics.md),
  [ADR 0017](../../../docs/adr/0017-deterministic-intersection-phase-rank-identity.md)
  and [ADR 0018](../../../docs/adr/0018-semantic-spline-2d-capability.md).
- [Verification-level contract](../../../geocedg/specs/operations/verification-levels.md).

# Geometric invariants and degeneracies

Implement the linked specification rather than duplicating mathematical truth
here. Require current outward-rounded local existence/uniqueness and complete
coverage of the selected normalized sign class. Preserve the distinction
between local admissibility and global completeness. Current actual floating
structural-function/gluing/composition evidence, not an ideal diagnostic curve
or rounded span cache, governs certification. Any missing proof or budget
exhaustion fails closed truthfully.

Preserve the source-backed interpolation and boundary equations, 3–32 point and
degree/work admission, weights, explicit domain/orientation and normal DAG
dependencies. Establish simple-knot C^(d-1) structurally and periodic seam jets
separately; keep numerical interpolation/backward error truthful. Certify the
same structural function through outward-enclosed exact numerator/denominator
coefficients. Do not reinterpret an approximate cache as exact authority.
Revalidate original equations without tolerance inflation. A singular/ill-
conditioned or inconsistent closure case must be reported, not silently admitted.

Correction A preserves the efficient binary64 path when original admission
passes, with a deterministic finite higher-precision ladder only where needed.
Use the exact binary64 inputs, explicit rounding/operation order and work caps;
never relax the original equations, pivot, residual, finiteness or structural
checks. Canonical double coefficients are preferred when they remain valid;
retained higher-precision derived authority needs demonstrated necessity and
normal command reconstruction, not new serialized coefficient state.

Correction B applies only to native structural SplineV2/supported R5 sources
against `REGULAR_POLYNOMIAL_IMPLICIT` stored exact coefficient authority;
historical circle/line/conic paths are unchanged. It treats floating
composition/refinement as discovery, not proof.
Certify Q(S(I)) and its derivative directly from the current structural source
with outward enclosures. Excluded intervals publish no root; a simple transverse
root needs existence, uniqueness and derivative exclusion of zero. Unknown
or multiple contacts remain nonmaterializable rich evidence. Preserve the two
genuine double contacts and reject the four spurious split candidates in the
historical implicit example without factorization, multiplicity alteration,
ledger suppression or tolerance inflation. The pair selector is unchanged.

Version changed derived-model/capability content signatures without assigning a
new durable locus identity merely for a reconstructed representation. Preserve
old-file reconstruction, current proof invalidation, R4/R5/R6/S1 consumers and
Classic Spline. Test exact jets, 20 structural cases, separate periodic cases,
native both/one-source knot roots, nearby distinct roots and transformed sources.
Record actual system dimensions, solve/enclosure work and budget failures.

At most one root per sign is eligible in one component product. Opposite germs
can both qualify; a `+,+,-` case may admit only its singleton class. Same-germ
multi-sheet and demonstrated monodromic cases remain rich-only. Structural
selectors survive regular parameter drift, projected-rank swaps and compatible
chart/knot/seam changes; no fixed parameter-displacement identity threshold.

Active/dormant/pair-quarantined recurrence retains the SAME point/ID/token.
Reactivation denotes the recurring semantic slot, not physical trajectory
identity across singularity. Recompute never creates new points. Explicit
incompatible durable replacement/disposal remains a G9A structural barrier;
ordinary temporary certificate/context loss does not permanently retire a slot.

# Compatibility and serialization

Extend the existing ledger with strict discriminated pair state and exact
two-source attachment/remapping. Preserve legacy one-source v1-v4 import and
R4 behavior; pair-bearing state requires a new strict version. Reject malformed,
down-versioned, source-mismatched or duplicate/conflicting pair data. Do not
interpret old rich-only pair diagnostic handles as new tokens.

Require native `.cedg` pair-point save/reopen in active/dormant/reactivated
states, copy/remap including reversed canonical source IDs, undo/redo, rename,
compatible/incompatible redefine and same-input path independence. Saved state
is not current proof. Keep the exact token auxiliary hidden and normal DAG-owned.

# Required tests and commands

Use explicit scoped DEV filters while implementing. Register G9S1-R1 in the
existing runtime phase registry and compose its focused verifier after R6.
Corrective execution order is DEV historical 25-point admission, DEV historical
implicit false split, structural/one-sided historical DEV, complete R1 DEV,
then PHASE A/B, COMPOSED and FULL. Earlier green PHASE and failed COMPOSED/DEV
records remain separate source cohorts; none substitutes for corrected evidence.
Use `tools/agent/verify-g9s1-r1-spline-pair-materialization.ps1` for deterministic
focused A/B, including exact required Java method inventory, fresh JUnit results,
numeric/certificate assertions, Checkstyle, scenarios, source/evidence hashes,
historical authority and both Git diff checks. Never substitute another phase's
PASS or an aggregate test count for these live assertions.

```powershell
.\tools\agent\verify.ps1 -Level PHASE -Phase G9S1-R1 -KeepBuildOutputs -LogDirectory artifacts/g9s1-r1/phase-a
.\tools\agent\verify.ps1 -Level PHASE -Phase G9S1-R1 -KeepBuildOutputs -LogDirectory artifacts/g9s1-r1/phase-b
.\tools\agent\verify.ps1 -Level COMPOSED -KeepBuildOutputs -LogDirectory artifacts/g9s1-r1/composed
.\tools\agent\verify.ps1 -Level FULL -CleanBuild -KeepBuildOutputs -LogDirectory artifacts/g9s1-r1/full-clean
```

Use fresh invocation-specific directories if any named directory already holds
evidence. Require identical canonical focused summaries. PHASE A/B may also use
the focused verifier's `-CanonicalSummaryPath`/`-CompareCanonicalSummaryPath`
contract with `-IncrementalBuild`; this is the same live phase authority.
Run the focused verifier with `-SkipBuild` only for static/incomplete checks.

The historical perimeter includes G8 pair/intersection authorities, G7 metrics,
G9A, G9U0/R1/R2/R3/R4/R5, G9S1, R6, G9X1/G5, legacy/scientific Locus,
frontend/profile, Classic, native persistence and relevant style. COMPOSED/FULL
must reuse current-run evidence where authorized, retaining every historical
live assertion; do not manually relaunch every historical Gradle suite.
Report requested scientific benchmark bodies separately from JUnit counts.

`VERIFICATION_INFRASTRUCTURE_IMPACT=UPDATE_REQUIRED`: the new phase registration
and orchestration require focused operational checks and FULL `-CleanBuild`.
The accepted terminal remains `<LEVEL> technical verification gates passed;
author approval is separate.` Do not rewrite it to mimic a historical terminal.
Freeze all writers during verification and use a fresh PowerShell process after
runtime-module edits. No cached/UP-TO-DATE mandatory Test execution is evidence.

# Required artifacts

New ADR/specification/prompt, matrix, actual source/API implementation record,
new scenario/evidence/hash manifest and candidate report; focused verifier,
phase/composed registration; modified-files registry; living roadmap/traceability
and justified user/developer guide updates. Logs belong only in ignored artifacts.

Record substantive `GUIDE_IMPACT`. Bootstrap review must inspect the existing
PowerShell/JDK/Gradle/dependency/packaging assumptions; no new dependency means
`BOOTSTRAP IMPACT — NO CHANGE REQUIRED` with rationale, not arbitrary bootstrap
edits. Existing Java arithmetic and a phase-map addition do not install tools.

Prepare manual cases using the current rich inspector, not new GUI:

1. Certified opposite-germ spline pair: inspect and materialize eligible roots,
   regular motion, same-input different paths, same-token binding, native reopen.
2. Same-germ/mixed ambiguity: only independently certified singleton classes
   materialize; no coordinate guess; existing claims become non-current and
   reactivate only under the recurring current selector; no automatic new points.
3. Preserved repeated-periodic witness: rich-only, with truthful diagnostics.

Keep `G9-R4-PERIODIC-QUARANTINE-NATIVE-ROUNDTRIP` OPEN / TRACKED. R1 pair
persistence is different from that missing R4 native-quarantine evidence.

# Stop conditions

Stop before broadening if useful materialization needs a different selector,
weaker numerical contract, arbitrary same-germ sheet labeling, history/proximity,
uncertified gluing/composition, frontend semantics or unauthorized migration.
Report independent blockers instead of relaxing evidence. Do not self-approve.

```text
Current G9S1-R1 = IMPLEMENTATION CANDIDATE — PENDING AUTHOR REVIEW
G9S1-R1-D2 DESIGN = AUTHOR APPROVED
DISPOSITION C = AUTHOR ACCEPTED
implementationStarted = true
implementationComplete = true
implementationAuthorized = true
correctionAAuthorized = true
correctionBAuthorized = true
correctionAResolved = true
correctionBResolved = true
selfApproved = false
authorApprovedPhase = false
passClaimed = false
manualAuthorSmoke = PENDING
G9U1 = DESIGN APPROVED / IMPLEMENTATION NOT AUTHORIZED
G9U1 reconciliation = BLOCKED UNTIL G9S1-R1 CLOSEOUT
G9-R4-PERIODIC-QUARANTINE-NATIVE-ROUNDTRIP = OPEN / TRACKED
```
